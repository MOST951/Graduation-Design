# 归档说明

此目录包含Java后端模块的备份副本。

## 说明

Java + Spring Boot 后端模块已恢复到项目根目录，作为项目的正式后端。

- `common/` → 已恢复到 `项目根目录/common/`
- `web-backend/` → 已恢复到 `项目根目录/web-backend/`
- `data-collector/` → 已恢复到 `项目根目录/data-collector/`
- `sentiment-analysis/` → 已恢复到 `项目根目录/sentiment-analysis/`
- `model-training/` → 已恢复到 `项目根目录/model-training/`

## 注意事项

- 此目录中的代码为备份副本，新功能开发请在根目录对应模块中进行
- Python NLP/Spark 脚本保留在 `backend-python/` 目录中
- 恢复时间：2026-04-04
