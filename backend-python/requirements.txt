# ====================================================================
# 微博情感分析系统 - Python依赖
# ====================================================================
# 安装命令: pip install -r requirements.txt
# ====================================================================

# ==================== 核心框架 ====================
Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0
# Swagger UI (论文 6.2.2 图6-9): 自动 OpenAPI 2.0 + UI
flasgger>=0.9.7

# ==================== 数据处理 ====================
numpy>=1.24.0
pandas>=2.0.0
requests==2.31.0
urllib3>=2.0.0

# ==================== 中文NLP ====================
jieba==0.42.1
opencc-python-reimplemented>=0.1.7  # 繁简转换（纯 Python，无 C 依赖）

# ==================== 大数据处理 ====================
pyspark==3.5.0
# WebHDFS 客户端: 论文 5.1 按日期分区上传原始数据到 HDFS /raw/dt=YYYY-MM-DD/
hdfs>=2.7.0
# 系统资源监控: /api/admin/system/metrics (无 psutil 时降级到 /proc, 但装上更稳)
psutil>=5.9.0

# ==================== 安全/加密 (必需) ====================
# api/admin.py 使用 Fernet 对配置加密, 缺失会导致 Flask 启动失败
cryptography>=41.0.0
# BCrypt: 论文 6.2.3 - 双后端共享 `users.password` 列, Java Spring Security
# 要求 BCrypt 格式 ($2a/$2b/$2y), 故 Python seed 默认用户时也必须写 BCrypt
bcrypt>=4.0.0

# ==================== BERT 深度学习 ====================
# 中文情感分析 (ChineseBERT) 必需
torch>=2.1.0
transformers>=4.35.0

# ==================== 数据库 ====================
pymysql>=1.1.0
DBUtils>=3.0.0

# --- 网页爬虫 (可选) ---
selenium>=4.15.0
# webdriver-manager>=4.0.0
